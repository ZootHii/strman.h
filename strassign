char *strassign(char *dest, const char *src, int dest_len){
	
	int new_len;
	int len = strlen(src);
	
	if (len < dest_len)
		new_len = len;
	else
		new_len = dest_len - 1;
		
	memmove(dest,src,new_len);
	
	dest[new_len] = '\0';
	
	return (dest);
}
